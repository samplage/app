{
  "name": "@samplage/desktop",
  "version": "1.0.0",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "start": "yarn compile && electron .",
    "clean": "shx rm -rf build && rm -rf dist && rm -rf node_modules",
    "compile": "tsc -b"
  },
  "devDependencies": {
    "@types/electron-is-dev": "1.1.1",
    "electron": "8.2.3",
    "electron-is-dev": "1.2.0"
  }
}
